version: '3'
services:
  website:
    image: nginx:alpine
    container_name: dashboard_famille
    volumes:
      - ./html:/usr/share/nginx/html
    ports:
      - "80:80"
    restart: unless-stopped
